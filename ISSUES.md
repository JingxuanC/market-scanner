# 未决问题清单（滚动更新）

> 每一条都带**证据**，不是主观判断。修完把状态改成 ✅ 并保留原始证据。

## A. 已修但仍值得记的（因为暴露了流程缺陷）

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| A1 | **候选池重复跑会累积旧行** → 池子不可用 | 同一 date 实测 39 行、权重合计 **1.3448** | ✅ 整日替换（写前 DELETE） |
| A2 | **`rank` 文案撒谎**（rank 是权重序，reason 写"反转20 前N"） | alpha 最高 0.3184 的票 rank=12 | ✅ rank=选择序，reason 各字段说真话 |
| A3 | **§8 风控未执行**（单票≤12%、单板块≤25%） | 代码里无约束逻辑 | ✅ water-filling 封顶；不可行时显式记账；板块约束在无行业源时**显式报未执行** |
| A4 | **测试给假信心**：A1 的"幂等"用例只测"同一只写两次"，没测"标的集变了" | — | ✅ 补 test_rewrite_same_date_replaces_whole_set |
| A5 | water-filling 第一版写成"截断+整体归一"→ 数学上永远不收敛 | 测试直接挂住 | ✅ 改标准 water-filling |
| A6 | hub 的 `portfolio_optimize` HRP **从未跑通过**（只读数组） | `np.fill_diagonal(dist.values, 0)` → ValueError | ✅ 已修（factor-miner 910b720） |
| A7 | hub 的 `ml_predict` 曾返回 `{"status":"ok","n_predicted":0}`（一只没算却报成功） | `_feature_names` 只在内存 | ✅ 特征名落 sidecar + 长度校验 + 拒绝 ok+0 |
| A8 | **我自己的错判**：看到两只票 ml 同值就说"ML 是常数" | 实测 **24 个不同取值**、因子引擎 64 键 59 非零 | ✅ 已纠正 |

## B. 未修（当前"结果逻辑不符预期"的主要来源）

### B1. 组合优化权重**完全不含 alpha**（最影响结果意义）
- **证据**：重验输出里**权重最大的那只，选择序是第 29/200 名**。HRP 只按相关性分配风险。
- **为什么没修**：hub 的 `portfolio_optimize(symbols, klines, method∈{hrp,equal,min_variance,mean_variance}, lookback)` **没有期望收益/alpha 入参**，§4.4 的 `max αᵀw − λwᵀΣw` hub 给不了。
- **建议**：在 scanner 本地实现 α 倾斜优化（scipy/cvxpy，带 §8 约束），或请 hub 给 `mean_variance` 加 `mu` 入参。**注意别假装已经有了。**

### B1'. α 倾斜权重：**机制已验证，但集中度未受控**（2026-09-15 实测）
- 已实现 `scanner/optimize.py::alpha_tilted_weights`（SLSQP 解 max αᵀw − λwᵀΣw，
  Σw=1、0≤w≤12%），并在 daily 里做成 `weighting="hrp"|"alpha_tilted"`（**默认仍 hrp**）。
- **机制验证通过**：同一批票同一截面，权重与选择序的秩相关 ρ 从
  **HRP −0.123** 提到 **α 倾斜 ~+0.99**（λ=0.25~1）。
- **但集中度调不动**：λ=0.25/0.5/1/2/5 全部是"前 5 合计 60%、前 10 合计 97~100%"，
  即后 20 只权重≈0 —— 这份"30 只组合"实际只有 ~10 只有仓位，**比 §8 设想的
  10–20 只分散更集中**。λ 调大还会让 ρ 退化（+0.44/+0.71 非单调）且 SLSQP 不收敛。
- **结论**：**不能拿它替换默认 HRP**。要修得换路子：协方差收缩（Ledoit-Wolf）、
  基数约束（最多持 N 只）、或把风险项改成"收益单位"的波动（而非归一化方差）。
- 中途还修了一处量纲 bug：α 做 z-score 后是 O(1)，而 w'Σw 只有 ~1e-2 量级 →
  风险项几乎不起作用（当时前 10 名占 100% 仓位、λ 大到起作用又不收敛）。
  已按对角线均值归一化。

### B1''. kronos IC 测：**测法已搭好，但单窗口不足以定论**（2026-09-15 实测）
- 测法：同一批票（流动性前 150）、同一 T 日（2026-09-07），kronos `forecast_batch(pred_len=5)`
  的 `expected_return_pct` vs h5 真实 5 日前向收益；**同时**算反转20 在同一批票上的 IC 作基准。
  运行成本：150 只批任务 **255 秒**（≈1.9s/只，与单只实测 1.4s 吻合），136 只成功、0 失败。
- **结果**：kronos IC=−0.0448 / rankIC=−0.0667；反转20 IC=−0.1431 / rankIC=−0.1192。
- **结论：不足以定论**。n=136 时 IC 标准误 ≈0.086，两个值都只离 0 约 0.5~1.7 SE。
  要定论必须**滚多窗口**（每次约 4 分钟）算均值 IC / ICIR / t 值。
- **但要警惕基准那条**：**反转因子在这个窗口是负 IC（−0.12）** → 该窗口是动量而非反转行情。
  当前选股清单正建立在反转之上 ⇒ **alpha 有强 regime 依赖**，动量周会反着做。

### B1'''. 多窗口 IC 实测（2026-09-15）——**修正 B1'' 的单窗口结论**
8 个非重叠 5 日窗口 × 每窗流动性前 ~110 只，kronos `forecast_batch(5)` vs h5 真实 5 日收益，
反转20 同批票同 T 作基准：

| | rankIC 均值 | 标准差 | ICIR | t (K=8) | IC>0 |
|---|---|---|---|---|---|
| **反转20** | **+0.1972** | 0.3282 | **+0.60** | **+1.70** | 62% |
| kronos | +0.0621 | 0.2546 | +0.24 | +0.69 | 75% |

逐窗口符号经常相反（09-07 kronos +0.099 / 反转 −0.073；07-27 kronos +0.541 / 反转 −0.281）。

- **修正**：B1'' 里"反转因子为负 IC ⇒ 当前选股依据不成立"是**单窗口**结论，过重。
  多窗口看反转**均值 +0.197（正向且可观）**，但**方差比均值还大 → t=1.70 不显著**。
  正确表述：**反转有正均值、方差极大、统计上不显著**。
- 反转明显强于 kronos（+0.197 vs +0.062）。
- **可测假设**：两者符号常相反 ⇒ 合成（z-score 后等权/IC 加权）可能提高 ICIR。
- **局限**：8 个窗口**不独立**（市场状态连续），有效样本量 < 8；要定论需 2–3 年、周频窗口。

### B1''''. 20 因子 IC 扫描（2026-09-15）——反转家族最强，管线自洽性验证通过
`factor_recent_ic`（60 日窗口、qlib 面板路径）批量扫 20 个经典价量因子，**有效 12/20**：

| 因子 | IC | | 因子 | IC |
|---|---|---|---|---|
| **rev60** | **+0.0739** | | nearhigh | −0.0352 |
| **ma60** | **−0.0671** | | rng20 | −0.0290 |
| **rev20**（当前在用） | **+0.0531** | | vtrend | −0.0266 |
| mom20 | −0.0531 | | rev10 | −0.0081 |
| gaprev | −0.0518 | | ma10 | +0.0079 |
| vratio | −0.0388 | | rev5 | +0.0032 |

- **自洽性验证通过**：`rev20=+0.0531` 与 `mom20=−0.0531` 精确对称 → IC 管线正确。
- **rev60 比 rev20 强 40%** → 反转窗口需选优（当前 20 日非最优）。
- **交叉印证**：`ma60`（均线偏离，独立实现）与 `rev60` 同向且同强 ⇒ "60 日均值回归"被两条独立路径证实。
- **有效信息集中在一个家族**（反转/趋势反向占前四），其余弱一个量级 ⇒ 多因子的增量主要来自**合成降噪**，不是找新家族。
- 全部 |IC| 在 0.03~0.07（日频因子正常量级）⇒ 单因子撑不起高确信度。
- **待修工具问题**：8 个失败因子都用了 `groupby(...).transform(lambda ...)`，沙箱疑似拦 `lambda` → 可用 `rolling().mean()` 的等价写法绕过。

### B1'''''. ✅ **合成因子通过统计门槛**（2026-09-15，今晚唯一可用的 alpha 结论）
同一把尺子（8 个非重叠 5 日窗口 × 每窗流动性前 ~115 只，rankIC vs 真实 5 日收益）：

| 信号 | rankIC 均值 | sd | ICIR | t (K=8) | IC>0 |
|---|---|---|---|---|---|
| rev20（原在用） | +0.1902 | 0.3327 | +0.57 | **+1.62**（不显著） | 62% |
| rev60 | +0.1486 | 0.1928 | +0.77 | **+2.18** | 88% |
| −ma60dev | +0.2004 | 0.2674 | +0.75 | **+2.12** | 62% |
| **composite**（三者截面 z-score 等权） | **+0.2190** | **0.2011** | **+1.09** | **+3.08** | **100%** |

- **合成降噪确实有效**：成分 sd 0.33/0.19/0.27 → 合成 **0.20**（低于所有成分），
  而均值**抬高**到 0.219（高于所有成分）⇒ ICIR 从 0.57 **翻倍到 1.09**。
- **t=3.08 显著**；**IC>0 占比 100%（8/8）**——比 t 值更硬：符号一次都没错。
- ⇒ **下一步就该把 scanner 的粗筛从 rev20 换成 composite**，并用同一 harness 复验端到端。

### B2. 单板块 ≤25% 约束**无法执行**
- **证据**：`industry_constraint_applied=false`，`industry_max_exposure=1.0`（全落在 UNKNOWN 桶）。
- **原因**：腾讯兜底（东财 clist 502 时的回退源）不提供行业字段。
- **建议**：恢复批量行业源（同 B3 的依赖），或引入静态行业映射文件。

### B3. 粗筛是**单因子极限尾部** + 无行业/市值中性化
- **证据**：3577 只 → 取 20 日跌幅最大的前 200（5.6% 尾部）；选出来风电/光储扎堆。
- **这正是 DESIGN §4.2 警告过的**："没有中性化，组合会隐性押注行业和市值风格"。
- **建议**：P1.5（中性化）+ 多因子合成，而不是单因子取尾部。

### B4. ML 无法上规模（快路径没有生产者）
- **证据**：`factor:*`（盘中命名空间）**0 个键**，factor-miner 的 17 个工具里**没有 `batch_compute`**；`ml_predict` 只能走 on-the-fly 因子计算 → **40 只 73 秒**，全市场不可行。
- **建议**：给 `factor:` 补一个按日批量写入者（Alpha158 快照批量落 Redis）。服务器已扩到 4c8G，够跑。

### B4.5 ML 排名**没有样本外依据**（2026-09-15 实测；**已补充：重训后 rank_ic 为负**）

**重训结果（已入队跑完，2026-09-15）**：`ml_train_rolling`（57 只 / 3380 样本 / 64 特征，约 5 秒）
- `ic = +0.0273`、**`rank_ic = -0.0474`（负）**、`sharpe = 2.3736`、`top10_return = 0.97%`
- `ml_metrics.last_train_at = 2026-09-15` ✅（OOS 指标已落库）

**结论**：横截面选股看的是 rank IC，**它是负的 → 模型排序方向与真实收益相反**。
所以当前 ML **不具备参与定序的资格**；`ic` 与 `rank_ic` 符号相反，还提示可能存在
**标签/符号层面**的问题（不是调超参能解决的）。scanner 已把排名依据默认改为 alpha
（`ranking=alpha`，见 daily.py），ML 只作记录。

#### 原始发现
- **证据**：`ml_metrics` 返回 `{model_exists: true, metrics: {}}` —— **空的**：这个 lgbm
  模型没有任何 OOS 记录（ic/rank_ic/sharpe 都没有）。
- **后果**：日频轨的**选择序是由它决定的**（`ranked` 按 ml 分降序），而 ML 分跨度只有
  ~0.007（-0.005~+0.0007），alpha 跨度 0.27~0.38。等于**用一个没被验证过的、极窄的信号
  来决定选谁**，把有实测 IC(+0.053) 的反转因子只用来做粗筛。
- **建议**：① 先 `ml_train_rolling` 重训，让 OOS 指标落进 `ml_metrics`；② 在那之前
  **排名应以已验证的 alpha 为准**，ML 只作为可选加权（并在 summary 里标明排名依据）。
- 相关：B1（权重不含 alpha）使"权重序 ≠ 选择序"——实测权重第 1 的票选择序是第 20。

### B5. 其它口径偏差
- 选股 as-of 用的是 h5 最近一个**完整**截面（今天退到 09-07）；数据修好后自动变 09-14。
- 可交易域用**当日成交额**，DESIGN §4.1 写的是 **20 日均成交额**。
- `alpha_source` 是**死参数**（docstring 说有开关，代码里没用）。

## C. 工程债
| # | 项 | 说明 |
|---|---|---|
| C1 | **用 `docker cp` 部署** | 容器一重建就回退 → 必须重建镜像固化（已把 deploy.sh 的 `builder prune -af` 改成保留 3G，避免每次重下 2GB） |
| C2 | 续抓目录曾被 `finally: rmtree` 清掉 | ✅ 只在自建临时目录上清；显式指定的目录由调用方管 |
| C3 | bind mount 挂在会被原子 swap 的目录的子目录上 | ✅ 改挂祖父目录 |
