# market-scanner · 设计文档

> 目标：把「选股 → 执行清单 → 记账归因」这条链做成一个能长期跑、能自我纠错的服务，
> 架在现有 hub MCP（数据 + 因子 + ML + kronos + 事件研究 + 因果记忆）之上。
>
> 适用对象：全职盯盘、**手动下单**的 A 股个人投资者（非机构、无自动交易通道）。

---

## 0. 一句话架构

```
日频轨（选股，服务底仓）   挂 h5/qlib 本地算 → 因子 → ML → 组合优化 → 候选池 + 下单清单
盘中轨（择时/风控，服务机动仓与做T）  走 hub MCP → 全市场快照 delta → 只告警"持仓+候选池"
记账轨（归因，服务迭代）     因果记忆 record_decision → 回填 → 周报 → 砍掉不赚钱的层
```

**关键分野**：日频轨走**共享磁盘**（6082×4302 的宽表不可能走 MCP 传输），
盘中轨走 **MCP**（只有快照/告警，payload 小）。这个分野决定了后面所有技术选型。

---

## 1. 硬约束（方案的边界条件，不可协商）

| 约束 | 含义 | 对设计的强制要求 |
|---|---|---|
| **T+1** | 当日买入次日才可卖 | 盘中买入无法当日纠错 → 机动仓必须**依托底仓**才能灵活（做T） |
| **手动下单** | 没有自动交易通道 | 一切产出必须是**人能照着点的清单**（代码/金额/限价/分笔），不是"权重向量" |
| **换手成本** | 往返约 0.1–0.3% | 底仓**周频**调仓（年成本 ~4%，而非日频的 ~19%） |
| **最低佣金 5 元/笔** | 小额单成本极高 | 单笔金额 ≥ 1–2 万，否则合并下单 |
| **程序化交易报告** | 2025-07-07 起施行 | **本方案不下单 → 无需报告**；若将来接自动下单，**首次交易前必须先报告** |
| **容量** | 个人资金，无容量压力 | 可做机构进不去的小票/低流动标的 ← **这是唯一的结构性优势** |

---

## 2. 分层策略结构（不要混在一起）

```
底仓 60–70%  日频多因子，尾盘建仓，周频调仓        ← 长期 alpha，省心，可复盘
机动仓 20–30% 竞价/盘中短线 + 依托底仓做T          ← 用时间换 alpha，小仓位、严纪律
现金 10%      机动与机会
```

**为什么必须分层**：三层混在一起，赚了不知道靠什么赚、亏了不知道哪层亏 → 无法迭代。
**每层独立记账、独立度量、独立止损**（见 §7、§9）。

### 做T 是全天有空的散户真正的变现方式

T+1 锁的是"当天买的"，**不锁"昨天及以前买的"**。所以有底仓即可日内回转：

| 方式 | 操作 | 风险 |
|---|---|---|
| **正T** | 早盘低点买 N（当日锁定）→ 盘中高点卖 N（用可卖底仓） | 买了继续跌 |
| **反T** | 冲高卖 N（可卖底仓）→ 尾盘低点买回 N | 卖飞，接不回来 |

- 单次价差目标 **≥ 0.5%**（否则覆盖不住 0.1–0.3% 往返成本）
- 单次做T 规模 **≤ 底仓的 1/3**
- **卖飞就放弃本次，不追高接回**（纪律写死）

---

## 3. 系统总览

```
┌───────────────────── market-scanner（新服务，mcp-suite 第 6 个服务）──────────────────────┐
│                                                                                          │
│  日频轨  ┌─ 20:10 每日 ───────────────────────────────────────────────┐                  │
│          │ 1. 可交易域过滤  ← 新增 get_a_trade_universe                │                  │
│          │ 2. 读 daily_pv_all.h5（本地挂载，6082 只）                   │                  │
│          │ 3. 因子：hub SOTA 因子库（经 factor_daily_compute 落 Redis） │                  │
│          │ 4. 打分：ml_predict（lgbm_rolling / master.pt）              │                  │
│          │ 5. 组合优化：portfolio_optimize(HRP/min-var) → 目标权重      │                  │
│          │ 6. 落库 candidate_pool（含权重、理由、因子快照）              │                  │
│          └──────────────────────────────────────────────────────────┘                  │
│                                                                                          │
│  执行层  ┌─ 14:35 每日 ─────────────────────────────────────────────┐                    │
│          │ 读 candidate_pool + positions → 算差额 → 生成「下单清单」   │                    │
│          │ 字段：代码/名称/方向/目标金额/股数/限价区间/分批/理由        │                    │
│          └──────────────────────────────────────────────────────┘                    │
│                                                                                          │
│  盘中轨  ┌─ 每 5 分钟 ──────────────────────────────────────────────┐                     │
│          │ 全市场快照（新增 get_a_market_snapshot）→ 算 delta          │                     │
│          │ 只对「持仓 + 候选池」匹配规则 → 告警（做T 提示 / 风控提示）    │                     │
│          └───────────────────────────────────────────────────────┘                     │
│                                                                                          │
│  记账轨  ┌─ 15:10 每日 + 周日 ───────────────────────────────────────┐                    │
│          │ record_decision(layer, context, decision) → 回填真实结果    │                    │
│          │ 周日：分层统计 + search_causal → 迭代结论                   │                    │
│          └───────────────────────────────────────────────────────┘                    │
└──────────────────────────────────────────────────────────────────────────────────────────┘
        ↑ 依赖 hub MCP（同一个 key / 同一套配额）    ↑ 依赖共享磁盘（h5 / qlib，只读）
```

---

## 4. 日频轨（选股主体）

### 4.1 可交易域（`get_a_trade_universe`，**hub 现在缺这个，必须新增**）

业界第一步，业余者最常跳过。剔除规则（先用保守默认，可调）：

| 规则 | 阈值 | 理由 |
|---|---|---|
| ST / *ST / 退市整理 | 剔除 | 涨跌幅 5%，流动性差 |
| 停牌 | 剔除 | 买不到 |
| 上市天数 | < 60 交易日 | 次新股波动无规律 |
| 20 日均成交额 | < 5000 万 | 冲击成本高、易被操纵 |
| 当日涨跌停一字板 | 剔除 | 次日大概率买不进 |
| 股价 | < 2 元 | 多为问题股 |
| 北交所 | 先剔除 | 流动性与规则不同，单独策略 |

→ 输出大约 **3000–4000 只**的可交易域，写进 `universe` 表（每日更新，保留历史以便复盘）。

**状态（2026-09-14 已实现并上线）**：`get_a_trade_universe` 在 hub 上可用，实测
扫 5544 只 → 通过 **5073**（剔除 ST 320 + 退市 151，逐只核对无误判），耗时 **17.6s**。
参数：`exclude_st` / `exclude_new_days`(默认 60) / `include_bj` / `min_amount_wan` /
`exclude_suspended` / `limit`，返回里带**逐项淘汰计数**（`excluded`）以便审计。

⚠️ **注意"数据集 ≠ 可交易域"**：本地 h5/qlib 有 6142 个 instrument，其中 588 是北交所、
其余还有指数/ETF；真正可交易的 A 股是 5553 只。所以 20:10 读 h5 时最新截面最多
≈5200 只，**不是 6082**——任何"全市场"分母都要说清用的是哪个集合。

### 4.2 因子

**不自建因子库**，用 hub 现成的：
- `factor_daily_compute`（每日 20:00 cron）→ SOTA 因子落 Redis `dfactor:{symbol}`
- 因子质量监控：`factor_recent_ic`（滚动 IC）+ `factor_tearsheet`（分组/衰减）
- 因子挖掘与准入：`run_factor_mining`（LLM 提假设 → 沙箱验值 → qlib 全量回测 → OOS 准入）

⚠️ **两个 Redis 命名空间别搞混**（2026-09-14 读代码确认）：
`dfactor:{symbol}` = **日频**（`factor_daily_compute` 20:00 cron 写，TTL 48h，
**athena 的 `factor/cache.go` 直接读它喂 WarRoom**）；`factor:{symbol}` = **盘中**
（TTL 300s，`batch_compute` 写）。而 `ml_predict` 的快路径读的是 **`factor:`（盘中）**，
所以日频轨调它大概率走 **on-the-fly 回退**（结果对、但慢）。
→ P2 开工前必须确认 `batch_compute`（`factor:`）到底有没有在跑。

**必须确认/补齐**：因子是否做了 **行业 + 市值中性化**。没有中性化，组合会隐性押注行业和市值风格，
回撤时会集中爆发（2024 年初小市值踩踏就是典型）。→ 列为 Phase 1 的**验收项**。

### 4.3 打分

```
z = ml_predict(因子)            # lgbm_rolling.pkl / master.pt，walk-forward 训练
alpha = 分位映射(z)             # 截面 rank → [-1, 1]
```
- 模型：日频轨只用**已训练**的模型；重训走 `ml_train_rolling`（周末跑，不占用交易时段）
- 验收：模型必须有 OOS 指标（`ml_metrics` 的 ic/rank_ic/sharpe），**只看 OOS，不看样本内**

### 4.4 组合优化（把信号变成权重）

```
输入：alpha 向量 + 协方差矩阵（从 daily_pv_all.h5 收益率算，取 universe 子集）
方法：portfolio_optimize(method="hrp") 起步（稳健、不用估期望）
      → 进阶：max αᵀw − λ·wᵀΣw  s.t. 见 §8 约束
输出：目标权重向量（不是 TopN 列表）
```

⚠️ **hub 真实接口与上式有差**（2026-09-14 查 schema）：
`portfolio_optimize(symbols, klines, method∈{hrp,equal,min_variance,mean_variance}, lookback=120)`
——它吃的是 **klines（价格→协方差）**，**没有 alpha/期望收益入参**。所以：
- **起步只能用 HRP / min_variance（纯风险配置）**，`lookback=120` 与 §12 一致；
- **alpha 倾斜（αᵀw）hub 现在给不了** → 要么在 scanner 本地实现（scipy/cvxpy，~50 行），
  要么请 hub 给 `mean_variance` 加 `mu` 入参。**降级为 P5 升级项**，别在 P2 假装有。

**这是"选股"真正的产出**。Top5 等权是 athena 的老做法，也是散户的典型做法——它把
"选股"和"配置"混成一步，结果单票风险巨大且无法控制行业暴露。

### 4.5 候选池落库（`candidate_pool`）

```sql
CREATE TABLE candidate_pool (
  date TEXT, symbol TEXT, name TEXT,
  alpha REAL,                     -- 原始打分
  target_weight REAL,             -- 组合优化给出的目标权重
  factor_snapshot TEXT,           -- 当日因子值 JSON（复盘用）
  ml_score REAL,
  rank INTEGER,
  reason TEXT,                    -- 可读理由（哪几个因子贡献大）
  PRIMARY KEY (date, symbol)
);
```

---

## 5. 执行层（手动下单的接口设计）

**这是本方案和机构方案最大的不同**：产出物不是权重，是**照着点就能下的清单**。

### 5.1 持仓来源

| 方案 | 实现 | 取舍 |
|---|---|---|
| **MVP：手工维护 `positions.json`** | `{symbol: {shares, cost}}`，每日收盘后你改一次 | 简单可靠，1 分钟 |
| 进阶：导入交割单 | 券商导出 CSV → 解析（athena 已有 `user_trades` 先例） | 准确，但要处理格式 |
| 远期：券商 API | 多数散户没有 | 不做 |

### 5.2 每日 14:35 生成的「下单清单」

```
┌────────┬──────┬────┬──────────┬───────┬─────────────┬──────────┐
│ 代码   │ 名称 │方向│ 目标金额 │ 股数  │ 限价区间    │ 理由     │
├────────┼──────┼────┼──────────┼───────┼─────────────┼──────────┤
│ sh600519│贵州茅台│ 买 │  18,000 │  100  │ 1275–1282   │ 反转+质量 │
│ sz300750│宁德时代│ 卖 │  12,000 │  200  │ 170.5–172   │ 权重下调 │
└────────┴──────┴────┴──────────┴───────┴─────────────┴──────────┘
合计买入 18,000 / 卖出 12,000 → 净 +6,000，现金充足
```

生成逻辑：
```
1. 读 candidate_pool 的目标权重 → 目标市值 = 总资产 × 目标权重
2. 读 positions → 当前市值
3. 差额 = 目标 − 当前 → 生成买卖单
4. 只生成 |差额| > max(3000, 总资产×0.5%) 的单（避免碎单被最低佣金吃掉）
5. 卖出单优先（回笼现金），买入单按 alpha 排序
6. 限价 = 现价 × (1 ± 0.3%)，并检查是否在涨跌停内
```

**执行时点**：14:35 生成 → **14:40–14:56 下单** → 避开 14:57–15:00 尾盘集合竞价。

---

## 6. 盘中轨（只告警，不选股）

### 6.1 为什么是全市场扫描，但**只推关于你的告警**

全市场 5000 只都推给你 = 信息轰炸 = 等于没信息。**只监控两个集合**：

```
监控集合 = 你的持仓 ∪ 候选池（Top 50）≈ 60–80 只
```

全市场快照的作用是**提供对照基准**（这只票在全市场的量比/资金流分位），而不是产生新标的。

### 6.2 告警规则（MVP 五条）

| 规则 | 触发 | 用途 |
|---|---|---|
| 持仓放量急跌 | 5 分钟跌幅 < −2% 且量比 > 2，且跑输大盘 1.5% | 风控：该不该做T 止损 |
| 持仓冲高回落 | 日内最高涨幅 > 4%，当前回落 > 2.5% | **反T 提示**（先卖后买） |
| 持仓急跌反弹 | 日内最低跌幅 < −4%，当前回升 > 2% | **正T 提示**（先买后卖） |
| 候选池量比突变 | 候选池标的量比 > 3 且 5 分钟涨幅 > 2%，且板块同步 | 机动仓机会 |
| 市场情绪恶化 | 全市场炸板率 > 40% 或跌停家数突增 | 降机动仓暴露 / 暂停做T |

**去重与静默**：同一标的同一条规则 30 分钟内只推一次；告警必须带"建议动作"，不带分析的告警是噪音。

### 6.3 数据来源与性能

```
get_a_market_snapshot()  → ~6000 只（东财 clist 分页，约 10 请求，em_get 自带节流）
     ↓ 5 分钟一次
本地内存保留上一 tick → 算 delta（量比变化、涨速、板块共振）
     ↓
只对监控集合匹配规则 → 告警（可选：微信推送，已有 wechat-mcp）
```

**成本**：纯数据 + 机械计算，**零 LLM**。这是刻意的——LLM 只在日频轨的可选环节（候选池 veto）出现。

---

## 7. 记账与归因轨（决定三年后你在哪）

### 7.1 task_tag 规范（必须严格，否则无法分层统计）

| task_tag | 归属 | 记录什么 |
|---|---|---|
| `desk-base-hold` | 底仓 | 每次建仓/调仓：因子值、目标权重、实际成交价 |
| `desk-mobile-hold` | 机动仓 | 竞价/盘中选择：触发规则、依据 |
| `desk-t0-reverse` | 做T（反T） | 卖出价、买回价、价差、是否卖飞 |
| `desk-t0-forward` | 做T（正T） | 买入价、卖出价、价差 |
| `desk-skip` | **录了但没执行** | 为什么没执行 + 事后收益 ← **最有价值的反馈** |

`context` 字段统一格式：`市场状态|板块|波动率|情绪`，例如 `震荡|新能源|高波动|炸板率25%`。
**同 task_tag + 同 context = 可比分支**，可以直接用 `counterfactual_query` 对比。

### 7.2 回填

```
T+1 / T+5 / T+10 / T+20 后自动回填真实收益 → record_decision(outcome=...)
做T 类：记录价差，扣除往返成本，得到净收益
未执行的决策（desk-skip）：记录"如果执行了会怎样" ← 用来度量执行纪律的价值
```

### 7.3 周日复盘（自动生成报告）

```
1. 分层统计：底仓 / 机动仓 / 做T 各自的收益、夏普、最大回撤、换手、成本占比
2. 底仓归因：收益 = beta(沪深300) + 因子 alpha；因子 alpha 是否显著
3. 因子 IC：本月各因子 IC/ICIR 与上月对比 → 衰减的因子降权
4. 决策质量：执行的决策 vs 录了没执行的决策，事后收益差
5. 纪律审计：止损是否执行、做T 是否违反"价差<0.5%不做"、是否追高
6. 结论：下月该加码/砍掉哪一层，哪几个因子退役
```

---

## 8. 风控参数（先写死，别靠情绪）

| 参数 | 默认值 | 说明 |
|---|---|---|
| 底仓单票上限 | 12% | 防止单票黑天鹅 |
| 底仓持仓数 | 10–20 只 | 分散度 |
| 机动仓单票上限 | 5% | 小仓位试错 |
| 做T 单次规模 | ≤ 底仓 1/3 | 控制卖飞风险 |
| 单日最大新增仓位 | 20% | 防止一天梭哈 |
| 底仓止损 | −8%（或因子失效） | 两者取先到 |
| 机动仓止损 | −3%（次日开盘执行） | T+1 限制下的最优解 |
| 组合回撤熔断 | −10% 减半仓；−15% 停手一周 | **写死在代码里** |
| 单板块暴露上限 | 25% | 防行业踩踏 |
| 做T 最小价差 | 0.5% | 低于此不做 |

---

## 9. 度量：怎么判断"这套到底有没有用"

**分三层度量，绝不合起来看总收益**：

| 层 | 基准 | 有效标准（连续 6 个月） |
|---|---|---|
| 底仓 | 中证 500 / 沪深 300 | 年化超额 > 8%，信息比 > 0.8，最大回撤 < 基准 |
| 机动仓 | 0 | 夏普 > 1.0，否则砍掉 |
| 做T | 0（扣成本后） | 净收益 > 0 且 胜率 > 55%，否则砍掉 |
| 整体 | 沪深 300 | 夏普 > 1.2，最大回撤 < 20% |

**判定纪律**：任何一层连续 3 个月不达标准 → **当月砍掉或减半**，不允许"再等等"。
这条比任何策略都重要。

---

## 10. 工程实施路线（按依赖排序）

| Phase | 交付物 | 依赖 | 验收标准 |
|---|---|---|---|
| **P0** ✅ | 修 hub cron（补 `MCP_LICENSE_KEY`）+ 修沙箱内存 | 无 | 20:00 `daily_compute` 真跑通，Redis 出现 `dfactor:*` |
| **P1** | hub 新增 2 工具：`get_a_trade_universe`、`get_a_market_snapshot` | P0 | 拉一次得 3000+ 可交易域 / 6000+ 快照，耗时 < 20s |
| **P1.5** | 确认因子中性化（行业+市值） | P0 | 抽出 20 个因子，检验与行业/市值暴露的相关性 |
| **P2** | scanner 日频轨 + 下单清单 | P1 | 20:10 自动出候选池；14:35 能生成可执行清单 |
| **P3** | 记账轨（task_tag + 回填 + 周报） | P2 | 每笔决策自动入库；周日出分层报告 |
| **P4** | 盘中告警轨 | P1 | 5 分钟 tick，只推持仓/候选池告警，30 分钟去重 |
| **P5** | 迭代（因子 IC 监控、组合优化升级、权重学习） | P3+P4 | 周报能驱动参数调整 |

---

## 13. P0 实施记录（2026-09-14）

**P0 实际是两个阻塞，不是一个**（第二个是查第一个时才暴露的）：

### P0-1：cron 缺 `MCP_LICENSE_KEY` ✅ 已修

```
cron_tasks.sh: line 8: MCP_LICENSE_KEY: 需要在 /opt/mcp-suite/.env 里配置 MCP_LICENSE_KEY
```

- 脚本第 7 行会自己 `set -a; . ./.env`，第 10 行 `MCP` 端点也是它写死的（`127.0.0.1:50053/mcp`，直连 factor-miner），
  **所以只缺 license key 这一个变量**
- 已写入 `.env`（0600），值取自 `/opt/athena-mcp/licenses/licenses.json` 的 `hjx-owner`
  （`daily_quota: 0` / `heavy_quota: 0` 即不限；因直连服务端口，**不走 quota-platform 配额层**）
- 影响面：`update_data`(15:40/18:10) / `daily_compute`(20:00) / `weekly_ic`(周日)
  **全部**因此在失败。日志显示 09-07 还成功过（`ic=0.0532`），之后断掉

### P0-2：沙箱内存 —— 算了 4300 天，只用 60 天 ✅ 已修

修好 env 后立刻暴露第二个阻塞：

```
numpy._core._exceptions._ArrayMemoryError: Unable to allocate 345. MiB
for an array with shape (6, 15095115) and data type float32
```

**误判过一版**：先查容器内存限额（无限制）、宿主（5.8G 可用）、容器占用（192MB）——
都不该失败。真因是**沙箱子进程的 `RLIMIT_AS = 2GB`**，而它算的是**虚拟地址空间**
（pandas/pytables 读宽表时 VA 远高于 RSS）。

根因不是内存不够，是**浪费**：
- `factor_recent_ic`：先在全量 15,095,115 行上算因子，**最后才 `tail` 到最近 60 天**
- `factor_daily_compute`：算全量，只取 `latest_row_per_instrument`（最新截面）
- 而 `_run_factor_df` 把全量 h5 **symlink 进沙箱** → 沙箱里 `pd.read_hdf()` 直接爆

修法（`factor-miner-mcp` @ `a38322f`）：
- 新增 `stage_h5_for_sandbox(data_h5, job_dir, window_days)`：截成最近 N 个交易日再给沙箱
- `WINDOW_DAYS` 默认 **400 交易日**（约 1.6 年），可用 `FACTOR_MINER_WINDOW_DAYS` 调；
  `0` 保持旧的 symlink 全量行为
- `sandbox.MAX_AS_BYTES` 改为可配（`FACTOR_MINER_MAX_AS_BYTES`，默认仍 2GB）
- `factor_execute(debug=False)` 也截窗，并在返回里**明示** `window_days`/`dataset`（不静默改评估语义）

效果：沙箱输入 15M 行 → ~1.4M 行，单次执行从"算 4300 天"变"算 400 天"，结果不变。

**这条给后续设计的启示**：hub 的因子链路一直"看起来有代码、实际没在跑"，
所以 **P1/P2 开工前必须先确认 P0 的产出真的落地**（Redis 里有 `dfactor:*`），
否则整套选股会建在空气上。

---

## 14. P0 现实核对（2026-09-14，与 §13 同源）

§13 那句"**P1/P2 开工前必须先确认 P0 的产出真的落地**"，当天就被验证是对的——
而且比预想的多查出 3 个缺陷（都不是"没写代码"，是"写了但没在跑/在悄悄跑坏"）：

| # | 缺陷 | 机制 | 修法 |
|---|---|---|---|
| 1 | **universe 静默退化** | 东财 clist 会把分页静默截断（`pz=500` 只回 100 行，异常时只回 **2 行**），旧代码 `if len(diff) < pz: break` 把短页当"列表到底了" → "全市场"退化成 2 只，而**全局日历照常前移**、只有这 2 只拿到新 bar。2026-09-08 起连续 4 个交易日 cn_data 每天只落 2 只票 | 按响应 `total` 翻页 + 与 `all.txt` 求并集 + 绝对下限 `MIN_UNIVERSE=2000` |
| 2 | **校验对"局部落库"失明** | `all.txt` 行数由 copytree 继承必然通过；抽样只抽"刚更新的那几只"（局部落库时必然抽中那几只）→ 校验永远通过 | 新增全量**覆盖面校验**（比对每只票 bin 行数与全局日历对齐比例）+ 抽样改为全 universe 随机 30 |
| 3 | **Redis 不可达但静默跳过** | `REDIS_URL=host.docker.internal:6379` 而 redis 只绑 127.0.0.1 → 连宿主 IP 必然被拒；写失败**静默降级**，`written=0` 也不报错 | 改走 compose 服务名 `redis://redis:6379/0` |

**两个过程性教训（我自己踩的，代价约 2 次全量抓取 ≈ 3 小时）**：
- **绝不在长任务运行时重启承载它的容器**：`docker restart` 会杀掉 `docker exec` 进去的任务；
- **续抓目录不能在正常退出时清理**：`finally: rmtree(csv_dir)` 让"校验失败"这条正常退出
  把刚抓好的 5206 个 CSV 全删了，续抓成了摆设。只在**自己创建的临时目录**上清理。

**P0 验收（唯一指标）**：Redis 出现 `dfactor:*` ≈ 5500 个，且 athena `GetDaily()` 复活。

---

## 11. 非目标（明确不做什么，同样重要）

- ❌ **不做自动下单**（手动 → 纪律在你自己手里；且一旦自动下单就必须走程序化交易报告）
- ❌ **不做日内高频**（对手是量化机构，你没有速度优势）
- ❌ **不从零自建因子库**（hub 有 SOTA 因子 + 挖掘流水线，重复造轮子）
- ❌ **不把 LLM 当选股主体**（可降级为候选池上的 veto/加权，且必须可关闭对比）
- ❌ **不全市场 5000 只都告警**（只告警持仓 + 候选池）
- ❌ **不在盘中用日频因子做买入决策**（因子是日频语义，盘中只有择时意义）

---

## 12. 待定与风险

| 项 | 状态 |
|---|---|
| 因子中性化是否已实现 | **待确认**（P1.5） |
| 组合优化的协方差估计窗口 | 待定（默认 120 日） |
| 持仓数据的录入方式 | MVP 手工，进阶交割单导入 |
| 微信告警推送 | 可复用已有 wechat-mcp |
| 数据源稳定性（东财/腾讯被限流） | **已实测**（2026-09-14）：东财 `push2/clist` 返 **502 Bad Gateway**、`82.push2` 与 `push2his` 直接断连；而 push2 单票 / push2ex 涨停池 / datacenter-web / 腾讯 / 新浪均正常。即故障是**逐端点**的。**推论：任何"全市场"结论都必须双源可交叉**（`get_a_trade_universe` 已做东财→腾讯回退） |
| 因子中性化是否已实现 | **已查实：没做**（`factor_eval.evaluate()` 只做 4 项结构检查；IC 是 `factor_backtest._daily_ic` 的原始截面 Pearson，无去极值/无中性化/非 rank IC）。补它需要**批量行业映射**，而批量行业源正是挂掉的 clist ← 与上一条同源风险 |
| 是否接自动下单 | 远期议题，**接之前必须先做程序化交易报告** |
